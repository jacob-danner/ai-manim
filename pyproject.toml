[project]
name = "ai-manim"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dspy>=2.6.27",
    "jupyter>=1.1.1",
    "manim>=0.19.0",
    "pillow>=11.3.0",
]

[dependency-groups]
dev = [
    "ruff>=0.12.7",
]
